name: first workflow
on:
  push:
    branches:
      - "main"
jobs:
  First-workflow-job:
    name: first job
    runs-on: ubuntu-latest
    steps:
      - name: checkout steps
        uses: actions/checkout@v4
      - name: run maven 
        run: mvn -f /home/runner/work/actionv1/actionv1/pom.xml
        run: mvn clean package
        shell: bash
